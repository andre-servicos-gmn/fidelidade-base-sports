# URL base da API administrativa. NUNCA hardcode no código — só aqui.
# Em dev, a API roda em http://localhost:8000 (uvicorn app.main:app).
# Em produção, aponte para a URL real da API.
VITE_API_URL=http://localhost:8000
